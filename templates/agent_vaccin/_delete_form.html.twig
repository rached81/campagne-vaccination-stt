<form method="post" action="{{ path('agent_vaccin_delete', {'id': agent_vaccin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agent_vaccin.id) }}">
    <button class="btn btn-danger" style="margin-left: 20px;margin-right20px; float:right"  >فسخ المسجل</button>
</form>
